module.exports = ({
    name: "np",
    code: `
    $description[Now im playing [$songInfo[title]]($songInfo[url]) - Added By: $username[$songInfo[userID]] - Duration: $songInfo[duration]]
    `
    });